[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "forge-deploy"
version = "0.1.0"
description = "Zero-dependency local PaaS with zero-downtime Blue-Green deployments"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
forge = "forge.cli:main"